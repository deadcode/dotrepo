#!/bin/bash

#xhost +local:
sudo docker run -it --rm \
       -v /tmp/.X11-unix:/tmp/.X11-unix \
       -v $HOME:/home/sdahiya/ \
       -e DISPLAY=$DISPLAY \
       --user 264401214 \
       --hostname $(hostname -f) \
       -w $(pwd) \
       nvim:0.4 \
       ~/bin/squashfs-root/AppRun $*

#xhost -local:
